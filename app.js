import * as solution from "./src/handlers/solutionHandler.js";

const print = console.log;

// TODO: Add chalk npm module for style in console (https://www.npmjs.com/package/chalk)

print(`Resultado del problema 1: ${solution.pE001}`);
print(`Resultado del problema 2: ${solution.pE002}`);
print(`Resultado del problema 3: ${solution.pE003}`);
print(`Resultado del problema 4: ${solution.pE004}`);
print(`Resultado del problema 5: ${solution.pE005}`);
print(`Resultado del problema 6: ${solution.pE006}`);
print(`Resultado del problema 7: ${solution.pE007}`);